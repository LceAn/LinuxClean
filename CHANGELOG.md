# 更新日志

所有重要的项目变更都将记录在此文件中。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)，
项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

---

## [未发布] - 2026-08-30

### 文档
- 新增 `ROADMAP.md`（未来更新计划），README 与 README_zh.md 增加文档索引。
- 本次为文档整理，脚本行为与测试未改动。

## [3.1] - 2026-08-04

### 改进
- 自定义模式将 systemd journal 与旧内核拆分为两个独立选项。
- 使用 `flock` 管理并发执行，避免异常退出后留下不可恢复的目录锁；无 `flock` 时保留兼容回退。
- 校验 `--user` 指定的本地用户，并对默认用户主目录去重。
- 仅删除超过保留时间的空目录，不再删除新建的空缓存目录。
- dry-run 候选预览使用固定容量 Top-N 缓冲区，大目录扫描不再保存全部路径。

### 安全修复
- 残留配置清理限制为版本化的 Linux 内核 image、headers 和 modules 包，不再清理任意已卸载软件包配置。
- 旧内核按 release 分组处理，完整保护运行中内核及最新备用内核，并一并清理旧 release 的 image、headers 与 modules 包。
- 用户缓存只从本机 `/etc/passwd` 取值，避免 NSS/LDAP 目录中的远程账号被默认纳入。
- 脚本可安全 `source`，测试调用不会意外执行主清理流程。

### 测试与工程化
- 新增纯函数单元测试，覆盖大小换算、locale、文件年龄、journal 参数与内核排序。
- 新增 GitHub Actions，在 push 和 pull request 上运行语法、ShellCheck、单元、CLI/i18n 及 full dry-run 检查。

---

## [3.0] - 2026-07-21

### ✨ 新增
- 新增 `--mode`、`--yes`、`--dry-run`、`--keep-days`、`--max-items`、`--journal-size`、`--user`、`--log-file` 等命令行参数。
- 新增非交互执行支持、进程锁、日志输出和版本/帮助入口。
- 新增按文件年龄清理 `/tmp` 与用户缓存，并汇总释放空间。
- 新增执行计划、阶段状态标签、候选大小排序、预计释放量和 dry-run 专用汇总。

### 🛡️ 安全修复
- 不再无条件删除 `/tmp` 或整个用户缓存目录，只处理超过保留天数的文件。
- 旧内核候选仅限版本化 `linux-image-*` 包，始终保护当前运行内核和最新备用内核。
- 统一校验日志大小、保留 dry-run 预览，并避免非交互任务因 `read` 卡死。
- 系统信息解析不依赖本地化的 `free`/`df` 输出。
- 修复 dry-run 显示“清理完成/释放 0 B”、journal 中英文混排和内核安装数量口径错误。

### 📖 文档与测试
- 重写中英文 README，补充安全模型、自动化示例和参数说明。
- 将 i18n 测试升级为可执行的语法、帮助、版本和错误参数 smoke test。

---

## [2.0] - 2026-03-03

### ✨ 新增
- 新增多种清理模式（快速/标准/完全/自定义）
- 新增彩色终端输出（红/绿/黄/蓝）
- 新增系统运行时间显示
- 新增清理模式选择菜单
- 新增清理后磁盘使用情况显示

### 🐛 修复
- 优化内核检测逻辑（使用 mapfile 数组）
- 改进错误处理（添加 2>/dev/null 静默）
- 修复用户缓存路径检测（使用 SUDO_USER）
- 修复 journal 日志大小显示

### 📖 文档
- 重写 README 文档
- 新增功能特性表格
- 新增清理模式对比表
- 新增常见问题 FAQ
- 新增使用示例

### 🔧 优化
- 代码结构重构（函数化）
- 添加脚本头部注释
- 统一代码风格
- 优化用户体验

---

## [1.1] - 2024-12-28

### 📖 文档
- 更新 README 文档
- 完善功能说明
- 更新注意事项

### 🐛 修复
- 修复小问题

---

## [1.0] - 2024-10-04

### ✨ 新增
- 初始版本发布
- 基础清理功能
  - 系统信息检测
  - 旧内核清理
  - 临时文件清理
  - APT 缓存清理
  - 用户缓存清理
  - systemd 日志清理
- 交互式操作
- 每步确认机制

---

## 版本说明

### v3.0
- 面向自动化和服务器场景的安全重构版本。
- 建议先使用 `--dry-run` 检查候选项，再使用 `--yes` 执行。

### v2.x / v1.x
- 早期交互式版本，保留在历史记录中。

---

## 贡献者

感谢所有为本项目做出贡献的开发者！

- [@LceAn](https://github.com/LceAn) - 创建者

---

*最后更新：2026-08-04*
